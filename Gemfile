# frozen_string_literal: true

# source is defined by our master Gemfile
git_source(:github) { |repo_name| "https://github.com/#{repo_name}.git" }

# Powered by GitHub
gem "github-pages", "221"

# Hoosegow requires posix/spawn for
# gem "posix-spawn"

# Staff-only Gems
gem "jekyll-geo-pattern"
gem "jekyll-octicons"
gem "jekyll-commonmark"
gem "jekyll-commonmark-ghpages"
gem "jekyll-paginate-v2"
gem "commonmarker"

# Whitelisted, but not versioned
gem "jekyll-include-cache"

# Support for Jekyll plugins
gem "nokogiri"

# Lock it down
# gem "hoosegow", :path => "."
# gem "excon"
# gem "octokit"

# Also include hoosegow's gem dependencies
# (except docker-api: we don't need to talk to docker from docker).
# gem "msgpack"
# gem "yajl-ruby", "1.4.1"
